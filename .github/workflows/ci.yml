name: CI

on:
  push:
  pull_request:

env:
  CARGO_TERM_COLOR: always

jobs:
  check:
    name: Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - name: Install system dependencies
        run: sudo apt-get update && sudo apt-get install -y protobuf-compiler libfontconfig1-dev pkg-config
      - uses: actions-rust-lang/setup-rust-toolchain@v1
      - run: cargo check --all-targets

  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - name: Install system dependencies
        run: sudo apt-get update && sudo apt-get install -y protobuf-compiler libfontconfig1-dev pkg-config
      - uses: actions-rust-lang/setup-rust-toolchain@v1
      - run: cargo test

  fmt:
    name: Format
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - name: Install system dependencies
        run: sudo apt-get update && sudo apt-get install -y protobuf-compiler libfontconfig1-dev pkg-config
      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          components: rustfmt
      - name: Check formatting
        run: |
          set -o pipefail
          if ! cargo fmt --all -- --check 2>&1 | tee fmt-output.txt; then
            echo "## Formatting Issues" >> $GITHUB_STEP_SUMMARY
            echo "Run \`cargo fmt --all\` to fix these issues:" >> $GITHUB_STEP_SUMMARY
            echo '```diff' >> $GITHUB_STEP_SUMMARY
            sed 's/\x1b\[[0-9;]*m//g' fmt-output.txt >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
            exit 1
          fi

  clippy:
    name: Clippy
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - name: Install system dependencies
        run: sudo apt-get update && sudo apt-get install -y protobuf-compiler libfontconfig1-dev pkg-config
      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          components: clippy
      - name: Run clippy
        run: |
          set -o pipefail
          if ! cargo clippy --all-targets --all-features -- -D warnings 2>&1 | tee clippy-output.txt; then
            echo "## Clippy Warnings" >> $GITHUB_STEP_SUMMARY
            echo "Run \`cargo clippy --fix --allow-dirty\` to fix auto-fixable issues:" >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
            sed 's/\x1b\[[0-9;]*m//g' clippy-output.txt >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
            exit 1
          fi
